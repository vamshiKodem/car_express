export interface BrandCheckboxItem {
  id: number;
  label: string;
  isChecked: boolean;
}

export interface RadioButtonsProps {
  id: number;
  value: string;
}

export interface BudgetList {
  id: number;
  budget: string;
}
